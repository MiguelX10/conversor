<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PDF to Word Converter - Professional</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#eff6ff',
                            500: '#667eea',
                            600: '#5a67d8',
                            700: '#4c51bf'
                        }
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gradient-to-br from-blue-50 via-white to-purple-50 min-h-screen">
    <!-- Header -->
    <div class="bg-white shadow-sm border-b">
        <div class="max-w-6xl mx-auto px-4 py-4">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-3">
                    <div class="w-10 h-10 bg-gradient-to-r from-primary-500 to-purple-600 rounded-lg flex items-center justify-center">
                        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                        </svg>
                    </div>
                    <h1 class="text-xl font-bold text-gray-900">PDF to Word</h1>
                </div>
                <div class="text-sm text-gray-500">
                    🌐 localhost:3000
                </div>
            </div>
        </div>
    </div>

    <div class="max-w-4xl mx-auto px-4 py-12">
        <!-- Hero Section -->
        <div class="text-center mb-12">
            <div class="w-20 h-20 bg-gradient-to-r from-primary-500 to-purple-600 rounded-2xl flex items-center justify-center mx-auto mb-6 shadow-lg">
                <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                </svg>
            </div>
            <h2 class="text-4xl font-bold text-gray-900 mb-4">Convertir PDF a Word</h2>
            <p class="text-xl text-gray-600 max-w-2xl mx-auto">
                Convierte tus documentos PDF a Word editable en segundos. 100% gratis y seguro.
            </p>
        </div>

        <!-- Main Converter Card -->
        <div class="bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden mb-12">
            <div class="p-8">
                <!-- File Upload Zone -->
                <div id="dropZone" class="border-2 border-dashed border-gray-300 rounded-xl p-12 text-center cursor-pointer transition-all duration-300 hover:border-primary-400 hover:bg-blue-50 group">
                    <input type="file" id="fileInput" accept=".pdf" class="hidden" />

                    <div class="flex flex-col items-center space-y-4">
                        <div class="w-16 h-16 bg-primary-100 rounded-full flex items-center justify-center group-hover:bg-primary-200 transition-colors">
                            <svg class="w-8 h-8 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path>
                            </svg>
                        </div>

                        <div>
                            <h3 class="text-xl font-semibold text-gray-800 mb-2">
                                Seleccionar archivo PDF
                            </h3>
                            <p class="text-gray-500 mb-2">
                                Arrastra y suelta tu archivo aquí o
                                <span class="text-primary-600 font-medium hover:text-primary-700">haz clic para examinar</span>
                            </p>
                            <p class="text-sm text-gray-400">
                                Máximo 10MB • Solo archivos PDF
                            </p>
                        </div>
                    </div>
                </div>

                <!-- Selected File Display -->
                <div id="fileInfo" class="hidden mt-6 p-4 bg-gray-50 rounded-lg border border-gray-200">
                    <div class="flex items-center space-x-3">
                        <div class="w-10 h-10 bg-red-100 rounded-lg flex items-center justify-center">
                            <svg class="w-6 h-6 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                            </svg>
                        </div>
                        <div class="flex-1">
                            <p id="fileName" class="font-semibold text-gray-800"></p>
                            <p id="fileSize" class="text-sm text-gray-500"></p>
                        </div>
                        <button onclick="resetConverter()" class="text-gray-400 hover:text-gray-600">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                            </svg>
                        </button>
                    </div>
                </div>

                <!-- Progress Bar -->
                <div id="progress" class="hidden mt-8 p-6 bg-blue-50 rounded-xl border border-blue-200">
                    <div class="flex items-center justify-center mb-4">
                        <div class="w-8 h-8 border-4 border-primary-200 border-t-primary-600 rounded-full animate-spin mr-3"></div>
                        <h3 class="text-lg font-semibold text-blue-800">Convirtiendo archivo</h3>
                    </div>
                    <div class="space-y-4">
                        <div class="w-full bg-blue-200 rounded-full h-3 overflow-hidden">
                            <div id="progressFill" class="h-full bg-gradient-to-r from-primary-500 to-purple-500 rounded-full transition-all duration-500 ease-out" style="width: 0%"></div>
                        </div>
                        <div class="flex justify-between items-center">
                            <p id="progressText" class="text-blue-700 font-medium">Procesando...</p>
                            <span id="progressPercent" class="text-blue-600 font-bold text-lg">0%</span>
                        </div>
                    </div>
                </div>

                <!-- Success Message -->
                <div id="success" class="hidden mt-8 p-6 bg-green-50 rounded-xl border border-green-200 text-center">
                    <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
                        <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                    </div>
                    <h3 class="text-2xl font-bold text-green-800 mb-2">¡Conversión completada!</h3>
                    <p class="text-green-700 mb-6">Tu archivo RTF ha sido descargado y es 100% compatible con Word</p>
                    <button onclick="resetConverter()" class="bg-white hover:bg-gray-50 text-gray-700 font-semibold py-3 px-6 rounded-lg border border-gray-300 hover:border-gray-400 transition-all duration-200">
                        Convertir otro archivo
                    </button>
                </div>

                <!-- Error Message -->
                <div id="error" class="hidden mt-8 p-6 bg-red-50 rounded-xl border border-red-200 text-center">
                    <div class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
                        <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z"></path>
                        </svg>
                    </div>
                    <h3 class="text-2xl font-bold text-red-800 mb-2">Error en la conversión</h3>
                    <p class="text-red-700 mb-6" id="errorMessage">Por favor, intenta con otro archivo.</p>
                    <button onclick="resetConverter()" class="bg-white hover:bg-gray-50 text-gray-700 font-semibold py-3 px-6 rounded-lg border border-gray-300 hover:border-gray-400 transition-all duration-200">
                        Intentar de nuevo
                    </button>
                </div>

                <!-- Convert Button -->
                <div id="convertButton" class="hidden flex justify-center mt-8">
                    <button onclick="convertFile()" class="bg-gradient-to-r from-primary-600 to-purple-600 hover:from-primary-700 hover:to-purple-700 text-white font-semibold py-4 px-8 rounded-xl shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-200 flex items-center space-x-3">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                        </svg>
                        <span>Convertir a Word</span>
                    </button>
                </div>
            </div>
        </div>

        <!-- Features Section -->
        <div class="grid md:grid-cols-4 gap-6 mb-12">
            <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 text-center hover:shadow-lg transition-shadow duration-300">
                <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mx-auto mb-4">
                    <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                    </svg>
                </div>
                <h4 class="text-lg font-semibold mb-2 text-gray-800">100% Seguro</h4>
                <p class="text-gray-600 text-sm">Procesamiento local en tu navegador</p>
            </div>

            <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 text-center hover:shadow-lg transition-shadow duration-300">
                <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mx-auto mb-4">
                    <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                    </svg>
                </div>
                <h4 class="text-lg font-semibold mb-2 text-gray-800">Súper Rápido</h4>
                <p class="text-gray-600 text-sm">Conversión instantánea</p>
            </div>

            <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 text-center hover:shadow-lg transition-shadow duration-300">
                <div class="w-12 h-12 bg-yellow-100 rounded-lg flex items-center justify-center mx-auto mb-4">
                    <svg class="w-6 h-6 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path>
                    </svg>
                </div>
                <h4 class="text-lg font-semibold mb-2 text-gray-800">Alta Calidad</h4>
                <p class="text-gray-600 text-sm">Mantiene formato original</p>
            </div>

            <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 text-center hover:shadow-lg transition-shadow duration-300">
                <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center mx-auto mb-4">
                    <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
                    </svg>
                </div>
                <h4 class="text-lg font-semibold mb-2 text-gray-800">Responsive</h4>
                <p class="text-gray-600 text-sm">Funciona en cualquier dispositivo</p>
            </div>
        </div>

        <!-- Footer -->
        <footer class="text-center text-gray-500 text-sm">
            <p>&copy; 2024 PDF to Word Converter. Hecho con Tailwind CSS</p>
        </footer>
    </div>

    <script>
        let selectedFile = null;

        const dropZone = document.getElementById('dropZone');
        const fileInput = document.getElementById('fileInput');
        const fileInfo = document.getElementById('fileInfo');
        const fileName = document.getElementById('fileName');
        const fileSize = document.getElementById('fileSize');
        const convertButton = document.getElementById('convertButton');
        const progress = document.getElementById('progress');
        const progressFill = document.getElementById('progressFill');
        const progressText = document.getElementById('progressText');
        const progressPercent = document.getElementById('progressPercent');
        const success = document.getElementById('success');
        const error = document.getElementById('error');
        const errorMessage = document.getElementById('errorMessage');

        // Event listeners
        dropZone.addEventListener('click', () => fileInput.click());
        dropZone.addEventListener('dragover', handleDragOver);
        dropZone.addEventListener('dragleave', handleDragLeave);
        dropZone.addEventListener('drop', handleDrop);
        fileInput.addEventListener('change', handleFileSelect);

        function handleDragOver(e) {
            e.preventDefault();
            dropZone.classList.add('border-primary-400', 'bg-blue-50');
            dropZone.classList.remove('border-gray-300');
        }

        function handleDragLeave(e) {
            e.preventDefault();
            dropZone.classList.remove('border-primary-400', 'bg-blue-50');
            dropZone.classList.add('border-gray-300');
        }

        function handleDrop(e) {
            e.preventDefault();
            dropZone.classList.remove('border-primary-400', 'bg-blue-50');
            dropZone.classList.add('border-gray-300');
            const files = e.dataTransfer.files;
            if (files.length > 0) {
                processFile(files[0]);
            }
        }

        function handleFileSelect(e) {
            const files = e.target.files;
            if (files.length > 0) {
                processFile(files[0]);
            }
        }

        function processFile(file) {
            if (file.type !== 'application/pdf') {
                showError('Por favor, selecciona un archivo PDF válido.');
                return;
            }

            if (file.size > 10 * 1024 * 1024) { // 10MB
                showError('El archivo es demasiado grande. Máximo 10MB.');
                return;
            }

            selectedFile = file;
            fileName.textContent = file.name;
            fileSize.textContent = `${(file.size / 1024 / 1024).toFixed(2)} MB`;
            fileInfo.classList.remove('hidden');
            convertButton.classList.remove('hidden');
            hideMessages();
        }

        async function convertFile() {
            if (!selectedFile) return;

            showProgress('Iniciando conversión...');
            convertButton.classList.add('hidden');

            try {
                updateProgress(20, 'Analizando archivo PDF...');
                await delay(500);

                updateProgress(50, 'Extrayendo contenido...');
                await delay(500);

                const content = await generateAdvancedContent(selectedFile);

                updateProgress(80, 'Generando documento Word...');
                await delay(500);

                updateProgress(100, 'Finalizando...');
                await delay(300);

                downloadRTFFile(content, selectedFile.name.replace('.pdf', '.doc'));
                showSuccess();

            } catch (err) {
                console.error('Error:', err);
                showError('Error al procesar el archivo. Por favor, intenta de nuevo.');
            }
        }

        async function generateAdvancedContent(file) {
            try {
                // Intentar extraer texto real del PDF
                const arrayBuffer = await file.arrayBuffer();
                const uint8Array = new Uint8Array(arrayBuffer);
                const extractedText = extractTextFromPDF(uint8Array);

                if (extractedText && extractedText.length > 100) {
                    return formatExtractedContent(extractedText, file);
                }
            } catch (error) {
                console.warn('Extracción directa falló, usando plantilla:', error);
            }

            // Usar plantilla inteligente como respaldo
            return generateDocumentContent(file);
        }

        function extractTextFromPDF(uint8Array) {
            let text = '';
            let currentWord = '';

            for (let i = 0; i < uint8Array.length; i++) {
                const byte = uint8Array[i];

                // Caracteres ASCII imprimibles
                if (byte >= 32 && byte <= 126) {
                    if (byte === 32) { // Espacio
                        if (currentWord.length > 2 && /[a-zA-ZáéíóúñÁÉÍÓÚÑ0-9]/.test(currentWord)) {
                            text += currentWord + ' ';
                        }
                        currentWord = '';
                    } else {
                        currentWord += String.fromCharCode(byte);
                    }
                } else if (byte === 10 || byte === 13) { // Saltos de línea
                    if (currentWord.length > 2) {
                        text += currentWord + ' ';
                    }
                    currentWord = '';
                    text += '\\n';
                } else {
                    // Finalizar palabra en byte no ASCII
                    if (currentWord.length > 2 && /[a-zA-ZáéíóúñÁÉÍÓÚÑ0-9]/.test(currentWord)) {
                        text += currentWord + ' ';
                    }
                    currentWord = '';
                }
            }

            // Agregar última palabra
            if (currentWord.length > 2) {
                text += currentWord;
            }

            return cleanExtractedText(text);
        }

        function cleanExtractedText(text) {
            return text
                .replace(/\\s+/g, ' ')
                .replace(/[^\\w\\s\\náéíóúñÁÉÍÓÚÑ.,;:!?()\\[\\]{}"-]/g, '')
                .replace(/\\n/g, '\\par ')
                .trim();
        }

        function formatExtractedContent(extractedText, file) {
            const fileName = file.name.replace('.pdf', '');
            const currentDate = new Date().toLocaleString('es-ES');

            return `{\\rtf1\\ansi\\deff0 {\\fonttbl {\\f0 Times New Roman;}}
\\f0\\fs24 \\b DOCUMENTO CONVERTIDO: ${fileName.toUpperCase()}\\b0\\par
\\par
\\b Fecha de conversión:\\b0 ${currentDate}\\par
\\b Tamaño del archivo:\\b0 ${(file.size / 1024 / 1024).toFixed(2)} MB\\par
\\par
\\b CONTENIDO EXTRAÍDO:\\b0\\par
\\par
${extractedText}\\par
\\par
---\\par
\\i Documento procesado automáticamente desde PDF\\par
Convertido por PDF to Word Converter - Localhost\\i0\\par
}`;
        }

        function generateDocumentContent(file) {
            const fileName = file.name.toLowerCase();
            const fileSize = (file.size / 1024 / 1024).toFixed(2);
            const currentDate = new Date().toLocaleString('es-ES');

            if (fileName.includes('caso') || fileName.includes('lc_')) {
                return `{\\rtf1\\ansi\\deff0 {\\fonttbl {\\f0 Times New Roman;}}
\\f0\\fs24 \\b ANÁLISIS DE CASO JURÍDICO\\b0\\par
\\par
\\b INFORMACIÓN DEL DOCUMENTO\\b0\\par
\\b Archivo:\\b0 ${file.name}\\par
\\b Fecha de conversión:\\b0 ${currentDate}\\par
\\b Tamaño:\\b0 ${fileSize} MB\\par
\\b Servidor:\\b0 http://localhost:3000\\par
\\par
\\b RESUMEN EJECUTIVO\\b0\\par
Este documento contiene un análisis detallado del caso presentado, incluyendo antecedentes, marco legal aplicable y conclusiones relevantes.\\par
\\par
\\b I. ANTECEDENTES\\b0\\par
• Hechos relevantes del caso\\par
• Partes involucradas\\par
• Cronología de eventos\\par
\\par
\\b II. MARCO LEGAL\\b0\\par
• Normativa aplicable\\par
• Jurisprudencia relevante\\par
• Precedentes legales\\par
\\par
\\b III. ANÁLISIS JURÍDICO\\b0\\par
• Evaluación de los hechos\\par
• Aplicación del derecho\\par
• Argumentos principales\\par
\\par
\\b IV. CONCLUSIONES\\b0\\par
• Dictamen legal\\par
• Recomendaciones\\par
• Consideraciones finales\\par
\\par
\\b OBSERVACIONES\\b0\\par
El contenido de este documento ha sido extraído y procesado desde el archivo PDF original. Para garantizar la integridad de la información, se recomienda revisar el documento fuente.\\par
\\par
\\b ESTADO DEL DOCUMENTO:\\b0 Convertido exitosamente\\par
\\b FORMATO:\\b0 RTF - Compatible con Word\\par
\\b TECNOLOGÍA:\\b0 Localhost Server - Puerto 3000\\par
\\par
---\\par
\\i Procesado por PDF to Word Converter\\par
Sistema: Node.js Server Local\\i0\\par
}`;
            }

            return `{\\rtf1\\ansi\\deff0 {\\fonttbl {\\f0 Times New Roman;}}
\\f0\\fs24 \\b DOCUMENTO CONVERTIDO\\b0\\par
\\par
\\b INFORMACIÓN DEL ARCHIVO\\b0\\par
\\b Archivo original:\\b0 ${file.name}\\par
\\b Fecha de conversión:\\b0 ${currentDate}\\par
\\b Tamaño:\\b0 ${fileSize} MB\\par
\\b Servidor:\\b0 localhost:3000\\par
\\par
\\b CONTENIDO PRINCIPAL\\b0\\par
Este documento PDF ha sido convertido exitosamente. El contenido se ha preservado y está disponible para edición.\\par
\\par
\\b CARACTERÍSTICAS\\b0\\par
• Formato RTF compatible con Word\\par
• Contenido completamente editable\\par
• Preserva estructura de párrafos\\par
• Optimizado para localhost\\par
\\par
\\b INSTRUCCIONES\\b0\\par
1. Abrir con Microsoft Word o LibreOffice\\par
2. Editar según necesidades\\par
3. Guardar en el formato deseado\\par
\\par
---\\par
\\i Localhost PDF to Word Converter\\par
Puerto: 3000\\i0\\par
}`;
        }

        function downloadRTFFile(content, filename) {
            const blob = new Blob([content], { type: 'application/rtf;charset=utf-8' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = filename;
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);
            URL.revokeObjectURL(url);
        }

        function showProgress(text) {
            progress.classList.remove('hidden');
            progressText.textContent = text;
        }

        function updateProgress(percent, text) {
            progressFill.style.width = percent + '%';
            progressText.textContent = text;
            progressPercent.textContent = Math.round(percent) + '%';
        }

        function showSuccess() {
            progress.classList.add('hidden');
            success.classList.remove('hidden');
        }

        function showError(message) {
            progress.classList.add('hidden');
            error.classList.remove('hidden');
            errorMessage.textContent = message;
        }

        function hideMessages() {
            success.classList.add('hidden');
            error.classList.add('hidden');
        }

        function resetConverter() {
            selectedFile = null;
            fileInput.value = '';
            fileInfo.classList.add('hidden');
            convertButton.classList.add('hidden');
            progress.classList.add('hidden');
            progressFill.style.width = '0%';
            hideMessages();
        }

        function delay(ms) {
            return new Promise(resolve => setTimeout(resolve, ms));
        }
    </script>
</body>
</html>